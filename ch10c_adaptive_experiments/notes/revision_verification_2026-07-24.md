# Chapter 10c revision verification

Date: 2026-07-24

## Prior-agent reconciliation

Commit history and chapter logs were reviewed before editing. The audit retains
the earlier fixes for collapsed hardness labels, the inverted rare-side pull,
and stale simulation artifacts. It supersedes the later relabelling of an ETT
warm start as the paper's full TS_C because the primary source and official code
show that Algorithm 1 never performs that initialization.

## Independent invariants

- Realized `m(q)` must equal every grid label in every replication.
- All non-optimal arms must have value 0.5 at every hardness level.
- The optimal arm must be included in phase 2.
- Successive Reject and Lattimore Algorithm 1 may not exceed horizon `T`.
- The reduced greedy-casino matrix must give observational means 0.15 and
  interventional means 0.30.
- Paper TS_C must beat unseeded context-conditional TS, and both must beat
  marginal Thompson sampling.

## Build and artifact status

- All three simulation components were recomputed from an empty cache as part
  of the final six-entry-point cold run.
- The embedded checks passed before output generation.
- The two TeX fragments and figure regenerated byte-identically to the
  committed artifacts. The refreshed stdout records cold computation rather
  than cache hits.
- The standalone chapter compiled in six pages with no undefined citations,
  undefined references, LaTeX errors, or minted failures.
- Pages 1, 2, 4, and 5 were inspected as rendered images. Equations, tables,
  figure labels, float order, and discussion placement were legible.
- The final independent shipping gate passed on the reconciled commit.
