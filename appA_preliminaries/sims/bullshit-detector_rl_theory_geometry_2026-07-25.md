# RL theory geometry figure audit

The artifact is diagram-only, so the score is capped at 25 percent.

1. Algorithm identity check
   Not applicable. The script draws fixed mathematical objects and runs no learning algorithm.

2. Mathematical fidelity
   Panel (a) computes the orthogonal projection from an inner product and the oblique projection as a line-subspace intersection. Panel (b) uses two normalized distributions and gives the behavior law zero mass where the target law has positive mass. Panel (c) draws the Euclidean unit ball and the quadratic region induced by \(F=\operatorname{diag}(4,0.25)\), with the natural direction computed by solving \(Fv=g\). Panel (d) evaluates the displayed rate functions directly.

3. Data integrity
   All coordinates and curves are generated by the script. No reported empirical result or cached input is used.

4. Comparison fairness
   The three rate curves equal one at the first iteration. Projection arrows share one target and one feature subspace. The trust-region directions share one objective gradient.

5. Theoretical sanity checks
   The orthogonal projection is shorter than the oblique projection in the displayed geometry. The coverage panel contains an explicit support violation. The Fisher ellipse is narrow along the high-curvature coordinate and wide along the low-curvature coordinate. Geometric decay eventually dominates both polynomial rates.

6. No information leakage
   Not applicable.

7. Reproducibility
   The script is deterministic. Default and `--plots-only` runs produced the same SHA-256 hash, `877cec619a1f2638eefcc7163b1989e93ba922bcc8c2086c9b0100313fa32c26`.

**Bullshit score: 10%** — A hostile reviewer could object that the projection and occupancy panels are schematic rather than calibrated to one named MDP, but every displayed relationship is computed from the stated geometry and the caption makes no empirical claim.
